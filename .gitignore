# Claude Code session artifacts (agent worktrees, local settings)
.claude/

# CI-generated static data (built from the EVE SDE at deploy time)
data/industry.json
data/ores.json

# the build tooling has no dependencies (sde.js is dependency-free)
tools/node_modules/

# test deps
tests/node_modules/
